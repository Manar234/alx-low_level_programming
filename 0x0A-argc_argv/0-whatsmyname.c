#include <stdio.h>
/**
 * main - prints the program name, followed by a new line
 * @agrc: int
 *@argv: list
 * Return: 0
 */
int main(int agrc, char const *argv[])
{
	(void)agrc;
	printf("%s\n", argv[0]);
	return (0);
}
